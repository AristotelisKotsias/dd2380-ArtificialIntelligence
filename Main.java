package TSP;

public class Main {

    public static void main(String[] args) {
        // Create cities
        int numStops;
        int popsize;
        int generation;
        int crossover_percentage;
        boolean is_SCX;
        int mutation_percentage;
        int mutation_limit;
        boolean isCostDistance;
        StopsMatrix stops[] = new StopsMatrix[numStops];


        // Loop to create random cities
       /* for (int cityIndex = 0; cityIndex < numStops; cityIndex++) {
            // Generate x,y position
            int latitude = (int) (100 * Math.random());
            int longitude = (int) (100 * Math.random());

            // Add city
            stops[cityIndex] = new StopsMatrix(latitude, longitude);
        }*/
    }
}
